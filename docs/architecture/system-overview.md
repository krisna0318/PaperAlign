# 系统架构概览

PaperAlign 采用本地 Web 架构：Vue 客户端连接本机 FastAPI 服务。文档流水线分为解析、分类、诊断、确认、格式化和验证六个阶段。

核心边界：

- 解析器只读取和建模；
- 语义解析器可以组合规则、模型和用户判断；
- 格式化器只消费已确认结构与版本化规则；
- 验证器独立检查内容保持和规则结果；
- Word 自动化是可选的最终完成模块，不是核心数据源。
